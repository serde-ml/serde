(executable
  (name record_test)
  (modules record_test)
  (preprocess (pps serde_derive))
  (libraries serde serde_json))

(executable
  (name variant_test)
  (modules variant_test)
  (preprocess (pps serde_derive))
  (libraries serde serde_json))
